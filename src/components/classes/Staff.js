class Staff extends User {
  constructor(id, name, email, status, type, password) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.status = status;
    this.type = type;
    this.password = password;
  }
}
